language: node_js
node_js: '10'
install: yarn install

script:
  - yarn build

jobs:
  include:
  - stage: release
    deploy:
      provider: script
      skip_cleanup: true
      script:
      - npx semantic-release
