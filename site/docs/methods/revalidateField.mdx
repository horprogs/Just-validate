---
sidebar_position: 5
---

# revalidateField

Method to trigger a field validation manually. Returns a promise with a `boolean` value - valid/invalid field.

```js
.revalidateField(field: string | HTMLInputElement).then(isValid => {})
```
