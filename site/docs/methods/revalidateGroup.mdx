---
sidebar_position: 14
---

# revalidateGroup

Method to trigger a group validation manually. Returns a promise with a `boolean` value - valid/invalid.

```js
.revalidateGroup(field: string | HTMLElement).then(isValid => {})
```
